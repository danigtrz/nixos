const launcher = () => Widget.Box({
	class_name: 'launcher',
	children: [
		Widget.Button({
			child: Widget.Icon('nixos-white-symbolic'),
			// on_primary_click: () => 
		})
	]
});

export default launcher;